{
  "name": "inventory-demand-dev-portal",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "dev:frontend": "vite --config vite.config.js --host 0.0.0.0",
    "build": "vite build --config vite.config.js",
    "preview": "vite preview --config vite.config.js --host 0.0.0.0 --port 3000",
    "sync:links": "node scripts/sync-links.mjs",
    "restart:dev": "node scripts/restart-dev.mjs",
    "prepare": "husky"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "vite": "^5.4.12"
  }
}
